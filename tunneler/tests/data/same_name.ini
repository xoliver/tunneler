# Config file containing a group and a tunnel with the same name.
[common]
default_user = mickey

[groups]
a =
    a
    b

[a]
name = this is tunnel a
local_port = 100
remote_port = 101
server = not.a.server

[b]
name = this is tunnel b
local_port = 101
remote_port = 102
server = not.b.server
